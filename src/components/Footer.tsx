import { memo } from 'react';

const Footer = memo(() => {
   return (
    <div
        className='w-full min-w-full min-h-40'
        style={{ background: 'rgba(41,52,74,255)' }}
    >

    </div>
   )   
});

export default Footer;